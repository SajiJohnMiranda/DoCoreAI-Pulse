name: DoCoreAI PyPI Test

on:
  workflow_dispatch:  # Enables manual trigger

jobs:
  test-docoreai:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "✅ Hello from DoCoreAI Test Runner!"
